import React from 'react'

function SideBar() {
    return (
        <nav className="p-1" style={{display: 'flex', flexDirection: 'column'}}>
            <div style={{flex: 1}}>
                Side bar
            </div>

        </nav>
    )
}

export default SideBar;